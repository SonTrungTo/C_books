#include <stdio.h>
#include <string.h>

#define       N       100

char *foreign[N], *english[N];

void translate(*a[], *b[]);

int main(void) {

  return 0;
}

void translate(*a[], *b[]) {
  int   i;

  *a[i]
}
