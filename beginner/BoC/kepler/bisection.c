#include "kepler.h"

dbl bisection(dbl f(dbl x), dbl a, dbl b) {
  dbl   m = (a + b) / 2;

  ++cnt;
  if (/* condition */) {
    /* code */
  } else if (/* condition */) {
    /* code */
  } else {
    /* code */
  }

}
