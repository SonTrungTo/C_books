// Initializing all elements of values (array) to 0
#include <stdio.h>

int main(void) {
  int values[10];
  int i;

  for (i = 0; i < 10; i++)
    printf("values[%i] = %i\n", i, values[i] = 0);

  return 0;
}
