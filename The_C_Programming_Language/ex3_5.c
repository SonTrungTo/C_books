#include <stdio.h>
#include <string.h>
#include <math.h>

#define     BIGVALUE    (int) pow(2, sizeof(int) * 8 - 1);
#define     abs(x)      ((x) < 0 ? -(x) : (x))

void        itob(int n, char s[], int b);
void        reverse(char s[]);

int main(void) {
  

  return 0;
}
